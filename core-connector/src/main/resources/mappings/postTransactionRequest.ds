{
    "action": "TRANSFER",
    "msisdn": ds.strings.substringAfter(payload.quoteRequest.body.payee.partyIdInfo.partyIdentifier, "-"),
    "reference_number": ds.strings.substringBefore(payload.quoteRequest.body.payee.partyIdInfo.partyIdentifier, "-"),
    "amount": payload.prepare.body.amount.amount
}
