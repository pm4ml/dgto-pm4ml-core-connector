{
    "action": "TRANSFER",
    "msisdn": payload.quoteRequest.body.payee.partyIdInfo.partyIdentifier[15:],
    "reference_number": payload.quoteRequest.body.payee.partyIdInfo.partyIdentifier[0:15],
    "amount": payload.prepare.body.amount.amount
}
